- debug:
    msg: "{{virtual_env}}"

- name: Pip install SQL
  include_tasks: "{{fn}}/Pip_install.yml"
  loop:
  - pymysql
  - sqlalchemy

- name: Pip install Web Scraping
  include_tasks: "{{fn}}/Pip_install.yml"
  loop:
  - beautifulsoup4
  - lxml
  - selenium
  - sqlalchemy
  - webdriver-manager 

